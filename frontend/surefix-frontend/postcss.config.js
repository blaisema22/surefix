module.exports = {
    plugins: {
        // Tailwind v4 Vite plugin handles processing; keep this empty or remove the file
        // unless you have other PostCSS plugins like 'postcss-import'.
    },
};